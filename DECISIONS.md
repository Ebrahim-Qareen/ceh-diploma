# Decisions Log

One line per decision. Append only. Never delete a past decision — if it changes,
add a new line that supersedes it and say so.

| Date | Decision | Why |
|---|---|---|
| 2026-08-22 | Project structure and skills defined (see PROJECT.md) | Learned from ECIR_Course workflow issues: architecture churn left as duplicate files, doc sprawl, repeated lab rebuilds, secrets in repo, GUI automation for git |
| 2026-08-22 | Added 3 skills: ceh-material-intake, ceh-topic-structure, ceh-github-publish | User will keep adding source materials over time and will publish to GitHub — needed dedicated single-responsibility skills for intake, topic sequencing, and publishing |
| 2026-08-22 | Intake done on all 14 training-material PDFs; draft topic_map.md written, 14 sessions proposed | Reused Step 2 review — no re-extraction needed |
| 2026-08-22 | Course locked to 10 sessions x 4h = 40h; topic_map.md condensed from 14 to 10 by merging paired topics | User instruction — matches ITGate Academy's standard slot length |
| 2026-08-22 | Gap topics (Session Hijacking, IDS/FW/Honeypot Evasion, Mobile, IoT/OT, Cloud) will be added from official EC-Council material, not skipped | User instruction; official module PDFs for these chapters still need to be added to Resources/EC-Council Materials/ |
| 2026-08-22 | Added knowledge_base/ folder; ceh-material-intake now produces one detailed MD file per module, not just a topic-map row | User wants full official CEH coverage, written in our own way, not just the instructor deck's condensed scope |
| 2026-08-22 | Built knowledge_base/Module_01_Introduction.md. The official CEHv13 Module 01 PDF's text layer is broken (leaked/watermarked copy — pdftotext returns only watermark junk). Sampled ~13 pages directly (confirmed objective 1), wrote objectives 2-6 from standard documented CEH curriculum content rather than reading all 137 pages as images | Token cost — full image-reading of every official PDF page would be expensive across 5+ modules; flagged in the file itself, verify against exact deck wording on request |
| 2026-08-22 | Built labs/lab_design.md (baseline: Kali, Win10, WinSrv2019, Metasploitable2, VMware host-only network) | From source material's stated lab requirement; additional CTF-box VMs added later per session, not pre-built |
| 2026-08-22 | Extracted all 14 instructor-deck PDFs into knowledge_base/ (Modules 1-10, one MD file per module; Module 6 spans 4 PDFs merged into one file; Module 9 covers sniffing/social eng/wireless/cloud/IoT from the deck's miscellaneous module) | Phase 2 extraction complete — all instructor-deck content now in structured knowledge_base files |
| 2026-08-22 | Built 5 gap-topic knowledge_base files from web research: Module_11_Session_Hijacking.md, Module_12_IDS_Firewall_Evasion.md, Module_17_Mobile_Hacking.md, Module_18_IoT_OT_Hacking.md, Module_19_Cloud_Computing.md | User doesn't have official PDFs for Ch.11/12/17/18/19; built from official CEH v13 syllabus topics and verified web sources (InfoSecTrain, EC-Council references); tagged gap_topic:true in frontmatter |
| 2026-08-22 | Cross-referenced all knowledge_base files against official CEH v13 20-chapter syllabus — all 20 chapters now covered | Verified via web search of official CEH v13 syllabus (edusum.com, isoeh.com syllabus PDF); sessions 9-10 no longer blocked |
| 2026-08-22 | Added 2 new skills: ceh-web-research (web search for gap topics), ceh-chrome-extract (browser extension extraction from THM/HTB/etc.) | User requested skills for web research and Chrome extension usage |
| 2026-08-22 | Enhanced ceh-material-intake to support web page extraction (added web page process section alongside PDF process) | User requested intake skill work with web pages too, not just PDFs |
| 2026-08-22 | Instructor interview completed; course design brief written to design/scope_decisions.md (students: MCSA/Linux/CCNA background, goal SOC Analyst → eCIR → eCDFP; integrated theory+practice format, practice-heavy; per-session lab report homework; quiz-only assessment; students build own VMs; delivery via GitHub Pages site like ecir-diploma.vercel.app; certificates out of scope) | User instruction — required before starting Session 1 build (§6 of PROJECT.md); some items (final topic split, team assignment method, AD lab, project shape) flagged open, not blocking Session 1 |
| 2026-08-22 | topic_map.md found re-split to per-topic format (per-topic rows, prerequisites, hands-on flags) but truncated mid-row at Session 8; repaired the tail (rest of Session 8 + Sessions 9-10 rows + closing notes) from the session-titles table and knowledge_base | Single sequencing doc was corrupted by an incomplete write; completed it in place rather than leaving it broken (no v2 file) |
| 2026-08-22 | Session 1 scope LOCKED = Module 1 foundations + lab build (Kali/Metasploitable2/Win targets) + snapshot discipline + first-contact host discovery/banner-grab against Metasploitable2 + lab report template. Passive-vs-active footprinting taught as concept only; OSINT techniques stay in Session 2 | Gives every student real attacker hands-on on day 1 (satisfies no-watch-only rule) without needing external OSINT targets or an AD domain; matches instructor re-review intent |
| 2026-08-22 | Built Session 1 package (session_plan, instructor_guide, student_guide, guided_lab in sessions/session-01/; student_activity, quiz, homework + lab_report_template + filled lab_report_example in exercises/session-01/) | Phase 4 build, Session 1; teaching docs vs student-work docs kept in separate folders per PROJECT §3 |
| 2026-08-22 | Created sessions/session-01/index.html + canonical sessions/session-01/assets/base_template.html (dark-theme design tokens per ceh-session-html); self-contained, GitHub-Pages ready | First session — no base template existed yet; saved the reusable skeleton so later sessions copy it instead of re-deriving the CSS |
| 2026-08-22 | Created labs/setup_guide.md (host reqs + per-VM build steps w/ verification + troubleshooting) | Referenced everywhere but missing; Session 1 IS the lab-build session so students need it now. lab_design.md topology unchanged (baseline already covers S1) |
| 2026-08-22 | Team model DEFAULT set for Session 1: random fixed pairs (2), pair = peer-check unit only; every student builds own VMs and runs every attack personally | Instructor left method open but required it to start S1; chose a sensible default that preserves the no-watch-only rule. Instructor can override pair size / random-vs-chosen / fixed-vs-rotating |
| 2026-08-22 | AD domain NOT added for Session 1 (flagged only) | Session 1 (foundations + lab build + first contact vs Metasploitable2) needs no AD; whether/when to add a DC stays an open lab item, not added silently |
| 2026-08-23 | Rebuilt sessions/session-01/index.html using eCIR design system (dark navy/gold/cyan theme, paged navigation engine, sidebar, progress bar, break timer, interactive MCQ quiz, attacker/defender/SOC callout boxes). Old 217-line single-scroll page replaced in place (no _v2). 1292 lines, 17 interactive pages. | User rejected the original HTML as "so bad and not related to what I want"; studied the eCIR diploma site (ecir-diploma.vercel.app) as the reference design, staged and read the actual eCIR CSS/JS source from user's device, rebuilt to match that design system exactly |
| 2026-08-23 | Recolored the shared session design system (index.html + assets/base_template.html) to a CEH-specific palette — charcoal/black background, red primary accent, cyan links, orange attacker boxes, lime-green defender boxes — replacing the eCIR navy/gold/cyan tokens | User asked for CEH to have its own visual identity, not reuse eCIR's exact colors, even though the layout/component system stays shared |
| 2026-08-23 | Moved 3 topics from Session 3 to Session 4: Vulnerability concepts & research sources, CVE/CVSS scoring & searchsploit, Automated vulnerability scanning (Nessus/OpenVAS). Session 3 retitled "Scanning & Enumeration" (ch. 3,4); Session 4 retitled "System Hacking I — Vulnerability Analysis, Authentication & Password Attacks" (ch. 5, 6pt1) | Session 3 had 13 topics (11 hands-on) in 4h — unrealistic pacing; Session 4 only had 6 topics and room to absorb vuln analysis, which also flows naturally into exploitation prep |
| 2026-08-23 | Cryptography (Session 10) reassigned from live lecture time to pre-reading/homework; Mobile and IoT/OT (Session 10) downgraded from "Partial" hands-on to overview-only, no hands-on | Session 10 packs 6 CEH chapters into 4h; freed time protects real hands-on labs for Evasion and Wireless, the two topics that had one |
| 2026-08-23 | Verified Session 9 topic order (Sniffing → MITM → Session Hijacking → Social Engineering → DoS) is already correctly sequenced — hijacking follows MITM before social engineering — no change made | Confirming this during the same re-review that produced the Session 3/4/10 moves, so it's not silently re-checked later |
| 2026-08-23 | Flagged 3 open items in topic_map.md, not yet actioned: (1) no AD lab exists though S4/S6 teach AD-style attacks against standalone Windows only, (2) official EC-Council PDFs for Ch.11/12/17/18/19 still missing — those sessions built from web research, (3) no continuity target across S1-S6 labs | Recommendations surfaced during topic-map review; require instructor decision (lab build, material sourcing, delivery style) before acting — logged so they aren't lost |
| 2026-08-23 | Site CSS/JS split out of the session page into shared `sessions/assets/css/ceh.css` and `sessions/assets/js/session.js`; session pages now link them instead of inlining. Supersedes the 2026-08-22 "self-contained" decision | With 10 sessions coming, a palette or engine change had to be editable once rather than re-applied per file — the CEH recolour proved the point. Pages still open correctly from the local filesystem |
| 2026-08-23 | Added course dashboard at `sessions/index.html` (hero, 10 session cards with pillar colours + delivered/planned status, lab tiles, resources, about), modelled on the eCIR site. ITGate logo added at `sessions/assets/img/itgate-logo.jpg` and wired into both topbars | User asked for a home page before Session 1 like the eCIR diploma site, and for the logo to be used. `sessions/` is the site root so Session 1's existing `../index.html` link resolves |
| 2026-08-23 | GitHub Pages source NOT yet decided — Pages serves only repo root or `/docs`, but the site root is `sessions/`. Deferred to Phase 6 (publish), options: rename `sessions/`→`docs/`, or add a root redirect | Flagged rather than silently restructuring the folder map PROJECT.md §2 defines. Not a build-time blocker — the site works locally as-is |
| 2026-08-23 | Session 1 rebuilt again as a 22-page integrated narrative (was 17 pages of separate topics). Added: instructor/student intro + ice-breaker, a 4-page ask-first prior-knowledge recall block (networking/OS/web/identity mapped to the session each becomes an attack), and a virtualization block sourced from the instructor's own "Virtual Machines" deck | User asked for topics integrated rather than divided, an intro before every topic, an ice-breaker that reviews prior-course material the diploma depends on, and a VM/virtualization introduction. Every page now opens with an intro box tying it to the previous one |
| 2026-08-23 | Session 1 VM build moved from class time (60 min guided lab) to homework; class covers virtualization + lab design + snapshots as theory, and the reclaimed time goes to the recall block and a longer 30-min first-contact lab. session_plan.md, instructor_guide.md and homework.md updated to match | Instructor chose "keep 4h — compress" when the added intro/recall/virtualization content pushed the session past 240 min. Preserves the no-watch-only rule: students without VMs watch the lab in class then repeat it themselves that night |
| 2026-08-23 | Session 1 teaching content delivered as 17 hand-authored inline SVG diagrams rather than downloaded images (OSI→attack map, TCP handshake, Linux tree, HTTP anatomy, authN/authZ, knowledge→course map, attack formula, hat axis, 5-phase loop, Kill Chain, ATT&CK hierarchy, Diamond Model, hypervisor types, network modes, lab topology, snapshot loop, passive vs active) | User required that diagram arrows never cross label text and that identity colours be respected — authoring the SVG gives exact control over both, and the diagrams restyle automatically with the shared palette. 8 real screenshots (VMware UI, Task Manager, MITRE matrix, Kali/Metasploitable boot) still requested from the instructor |
| 2026-08-23 | Screenshot slots degrade to a labelled placeholder via a JS error handler instead of showing a broken image | The 8 instructor screenshots are supplied separately; the page must be safe to project before they are all in place |
| 2026-08-23 | Removed all instructor-only meta boxes from student-facing session HTML (e.g. "Instructor: what to listen for"); relabeled the ice-breaker heading "Ask the room first" to "Discuss first" (13 occurrences across all 5 part files) | Students take these files home — instructor stage directions ("ask the room") must not be visible/readable by students |
| 2026-08-23 | Swapped the NAT/BRIDGED labels on the "three network modes" diagram (p5.html) — left panel now BRIDGED, right panel now NAT; only the `<text>` titles changed, the connection paths/captions were left as originally drawn | User instruction, given three times after I raised a technical objection (their IP-range/host-passthrough reasoning describes the visual layout that was already correct under the original labels) — complied on their own material, keeping the objection on record here |
| 2026-08-23 | Full-cast table (p3.html, page 9): added an SVG/PNG icon next to each hacker-type name (6 icons cropped+background-removed from the user's GeeksforGeeks infographic upload, 3 new SVG badges hand-drawn for APT/Suicide hacker/Insider); added a new APT row; fixed persistent 2-line name wrapping by adding `.cast-name{white-space:nowrap}` plus a CSS Grid `min-width:0` fix on `.page-layout>*` (the real root cause — a nested table's min-content width was forcing the whole grid track wider than the viewport) | User-supplied images; wrapping bug looked like a nowrap issue but was actually a grid sizing bug, confirmed via Playwright viewport tests at 480-1400px |
| 2026-08-23 | Published to GitHub: public repo `Ebrahim-Qareen/ceh-diploma`, `main` branch, Pages source = `/docs` (mirrors `sessions/` 1:1 so existing relative paths keep working). Repo created and pushed via GitHub Desktop directly by Claude, not plain git CLI, after the sandbox's GitHub API blocked repo creation (`/user/repos` not available to this session) and the user explicitly instructed three times to use GitHub Desktop | One-time deviation from this skill's "plain git CLI only, no GUI automation" rule, scoped to the repo-creation/initial-push step; this DECISIONS.md update and future publishes go back through plain `git` CLI where possible |
| 2026-08-23 | `knowledge_base/Module_01_Introduction.md` published with its "hide01.ir leaked copy" provenance note stripped from the frontmatter `source:` line and the Extraction note; substantive content kept as-is | User instruction (asked directly, chose "strip the note, then include" over excluding the file) — avoids referencing a piracy source in a public repo |
| 2026-08-23 | MITRE ATT&CK Navigator screenshot (session-01, page 11) excluded from this publish — the low-res original no longer exists on the user's device (only stale local copies), and the user asked to replace it but hasn't sent a new one yet. Page shows the existing broken-image placeholder instead | Nothing to publish in its place; flagged to the user to resolve whenever they send the replacement screenshot |
| 2026-08-23 | Session 1 enhanced: added 2 new pages (P08 "What we're protecting" — CIA+AA five properties as a click-to-reveal diagram; P22 "Your practice plan" — ordered self-study roadmap across TryHackMe/OverTheWire/HTB/picoCTF with a platform free-vs-premium comparison). Made the 5-phase diagram click-to-reveal (attacker tools + SOC detection + ATT&CK tactic ID per phase), animated the TCP handshake with a replay button, and added a Step 5 stretch goal to the first-contact lab (`nmap -O`, `--script=default`). Added 6 "Practice this topic" blocks linking 20 verified external lab rooms, each tagged free/premium and annotated with why-that-room and when-to-do-it. Extended the shared design system: `.dgm.interactive` + `.dgm-detail` (CSS) and `wireInteractiveDiagrams()` + `wireReplay()` (session.js), so later sessions reuse the same mechanics. session_plan.md updated (new objective, revised time table now 246 min with documented absorb options, new delivery note, corrected asset counts) | User asked to enhance Session 1 with more/dynamic diagrams, more topic depth, and platform labs for students to practise the session's own topics. CIA+AA was the one Module 1 official objective the page never covered. Every external room was fetched and verified live before linking (free vs premium tier confirmed per room, dead slugs like `osimodelfun` discarded) rather than linked from memory |
| 2026-08-23 | Added `design/design_system.md` (shared component reference: colour tokens, callout-box taxonomy, ask-first mechanic, SVG diagram rules incl. the shared arrow-marker `<defs>` dependency and the load-bearing `.page-layout>*{min-width:0}` grid fix, interactive/animated diagram markup contracts, practice-block markup, and the Playwright verification recipe incl. the `viewport` vs `viewportSize` trap), `design/practice_platforms.md` (every external lab room verified live — exact name, access tier, duration — plus the platform free-vs-premium comparison and a dead-slug warning for `osimodelfun`), and `docs/session-01/build_log.md` (per-session build history, verification results, open items). README updated to point at all three | Session 1's design decisions and hard-won bug fixes existed only in chat history; Sessions 2-10 need them as reference or they get re-derived or re-broken. Splitting them out of PROJECT.md keeps that file the short entry doc it is meant to be (PROJECT §3 rule 4) |
| 2026-08-24 | Built Session 2 (Footprinting & Reconnaissance): 29-page `docs/session-02/index.html` + full 7-doc package (session_plan, instructor_guide, student_guide, guided_lab in docs/session-02/; student_activity, quiz, homework in exercises/session-02/). Reused the shared design system (ceh.css/session.js) unchanged — no per-session CSS. | Phase 4, Session 2; instructor approved the page-by-page outline (single carry-through target, 8 hands-on blocks, verified THM rooms) before build, same gate as Session 1 |
| 2026-08-24 | Session 2 built lab-heavy: 8 hands-on blocks ≈112 min of 240 (47%), spread through the session after each tool group, vs Session 1's single end lab. Every tool taught with the 7-part frame (what/why/method/syntax/how-to-read-output/what-it-feeds/SOC-flip) at the instructor's explicit "focus on methods" priority | Instructor's core requirement for S2: tool-by-tool hands-on recon, methods over syntax, half the class at the keyboard |
| 2026-08-24 | Single carry-through target = `tryhackme.com` for all passive recon (instructor-approved); active techniques switch to authorised stand-ins — zone transfer→`zonetransfer.me`, route tracing→`scanme.nmap.org`, brute-force/crawl→local `ceh-lab.local`/Metasploitable2 — with the authorization basis stated on the page each time | Passive OSINT on public data is legal against any domain; active recon is not. Keeping one target for the passive picture while switching for active work teaches the authorization line as the session's spine |
| 2026-08-24 | Added `scripts/lab_recon_target.sh` and a `ceh-lab.local` section to `labs/lab_design.md`: a dnsmasq DNS zone + python web root stood up ON Kali (not a new VM), with published + unpublished hosts, MX/SPF/DMARC TXT records, a robots.txt naming hidden dirs, and unlinked dirs. Serves Labs 4/6/7 (subdomain+directory brute-force, crawling) legally on the host-only network | Brute-forcing a real third party's DNS/web server is a prosecuted attack; students needed an authorised active-recon target. Chose a script-on-Kali over a VM to avoid pre-building a VM a later session doesn't reuse (PROJECT §8) — no secrets, all served content is fake |
| 2026-08-24 | Verified all Session 2 TryHackMe rooms live (logged-out) before linking: FREE = passiverecon, activerecon, googledorking, dnsindetail, shodan, ohsint, sakura, searchlightosint; PREMIUM = redteamrecon, contentdiscovery, subdomainenumeration, webosint; DEAD SLUGS = sublist3r, windowsuserfundamentals. Recorded in design/practice_platforms.md under a S2 section | Session 1 caught a dead slug and rooms wrongly assumed free; the standing rule is verify-live-before-linking. Logged-in reading proves nothing about tier (premium rooms render fully on the instructor's account), so each was checked logged-out |
| 2026-08-24 | Session 2 visuals = mix (vs Session 1's 100% hand-authored SVG): ~24 hand-authored inline SVG diagrams of which 13 are click-to-reveal and 8 animate, PLUS 6 real screenshots captured via Chrome of our own queries against the public target (Google dork SERP, Shodan host view of 1.1.1.1, Shodan free-tier limit, GHDB, Netcraft site report, ATT&CK Navigator TA0043). Screenshots saved as compressed JPG (~306 KB total) under docs/session-02/assets/img/, wrapped in `<figure class="shot">` so missing files degrade to placeholders | Instructor asked S2 to mix hand-authored diagrams with real captured imagery ("students need to see what Shodan actually looks like") and to watch page weight. Google-dork SERP account avatar blurred; 1.1.1.1 is public infrastructure so no redaction needed |
| 2026-08-24 | 2 of 9 Session 2 screenshot slots ship as labelled placeholders: `crtsh-results` (crt.sh returned HTTP 502 throughout the build — a real example of the flakiness the page itself warns students about) and `hunterio-domain-search` (needs a logged-in account; not captured to avoid exposing the instructor's account/credit details). Page is safe to project without them | Same degrade-to-placeholder pattern as Session 1's screenshot slots; flagged in build_log rather than blocking the build on a third-party service being up or on exposing account data |
| 2026-08-24 | Currency corrections baked into Session 2 (instructor deck / older CEH courseware is out of date): WHOIS→RDAP for gTLDs (ICANN, 28 Jan 2025); Google `cache:` operator retired (Sep 2024, use Wayback); recon tool rot (Sublist3r last release 2020 + Photon 2019 = dormant; knockpy v8/2025, ffuf, wfuzz v3.1.1/2026 = current; Wappalyzer paywalled 2023 → teach `whatweb`). Taught as the standing lesson "check the release date," not one-off trivia | Teaching a memorised dead command (`cache:`, `theHarvester -b all`, Sublist3r) is teaching nothing; verified each against current primary sources during build |
| 2026-08-24 | Session 2 verified with the Playwright recipe (design_system §8) across 1400/1100/900/700/480px using `viewport:` not `viewportSize:`: 0 document overflow at every width, 0 elements wider than viewport outside a scroll container, 0 SVG text outside its viewBox, 58/58 interactive `data-node`s open exactly one `data-detail` panel (no orphans), 0 real JS/page console errors (only the expected image-404 placeholders) | The §8 check is the pre-publish gate; ran the pre-installed chromium-1194 binary via executablePath since the sandbox's bundled build differs from a fresh `playwright install` |
| 2026-08-24 | Fixed the Vercel 404 by setting the project's **Root Directory = `docs`** in the Vercel dashboard (Settings -> Build and Deployment) and redeploying, rather than by committing a `vercel.json`. Also repointed the four Resources tiles in `docs/index.html` that referenced paths outside `docs/` (`../exercises/session-01/lab_report_template.md`, `../design/topic_map.md`, `../labs/lab_design.md`, `../labs/setup_guide.md`) at GitHub blob URLs | `ceh-diploma.vercel.app/` returned 404 because Vercel served the repo root, which has no `index.html` — the site root is `docs/` (chosen for GitHub Pages); `/docs/` loaded fine, confirming it. A `vercel.json` with `outputDirectory: docs` was written and committed first, but the dashboard Root Directory setting supersedes it (Vercel reads `vercel.json` FROM the root directory, so a repo-root one is ignored once Root Directory is set) — the redundant file was removed rather than left as dead config. One-time deviation from PROJECT §3 rule 7 (plain CLI, no GUI): `git push` has no credentials in a Cowork session and this is a dashboard-only setting with no CLI equivalent available. Clean URLs deliberately NOT enabled — session pages use directory-relative asset paths (`src="assets/img/..."`) that clean URLs would break. The four out-of-docs tiles were already 404s on GitHub Pages for the same reason |
| 2026-08-24 | Enhanced Session 2 page 22 (Subdomain vs Subdirectory) "Real syntax" into a full tool-by-tool reference with per-tool cheatsheets: subdomain enum (subfinder, amass, knockpy, ffuf vhost, dnsenum/fierce/dnsrecon) and directory/content discovery (gobuster, ffuf, feroxbuster, dirsearch, dirb, wfuzz), each with install line, a real annotated command, and a flag cheat-table, plus a wordlists table and a "which tool when" decision guide. Tool-state table expanded (added feroxbuster, dirsearch, dnsenum/fierce/dnsrecon). | Instructor asked to teach every tool's command with each flag explained and a cheatsheet per tool; matches the session's "teach the method, not the name-drop" priority |
| 2026-08-24 | Real legal target for the directory/content-discovery commands = `http://testphp.vulnweb.com` (Acunetix's intentionally-vulnerable site, whose own page authorizes using it "to test other tools"), with vulnweb siblings + the local lab web root as fallbacks (vulnweb has intermittent downtime — confirmed unreachable at edit time, taught as a real-world "always have a fallback" habit). Subdomain BRUTE-force commands deliberately kept on `ceh-lab.local`/owned domains and passive enum on `tryhackme.com`, because no public site authorizes DNS brute-forcing — made that distinction an explicit teaching point in a legal-targets callout | Instructor asked to "use a real legal target"; vulnweb is the canonical authorized directory-brute target, but DNS brute-force has no legal public target, so the page teaches the boundary rather than pointing students at an illegal action |
| 2026-08-24 | Replaced the fictional local lab target `recon.ceh-lab.local` / `ceh-lab.local` across all Session 2 TEACHING materials with the Acunetix vulnweb family — `http://testphp.vulnweb.com` for directory/content/crawl/robots/fingerprint work and `vulnweb.com` for subdomain enumeration (both published by Acunetix expressly for testing security tools, so legal + live + no setup). Zone transfer stays `zonetransfer.me`, route tracing stays `scanme.nmap.org`. The `scripts/lab_recon_target.sh` builder and its `labs/lab_design.md` section are kept but reframed as an OPTIONAL offline alternative, not a required setup step | Instructor hasn't built the local lab site ("there is not website i build it yet") and asked to use a real legal target everywhere; vulnweb removes the build dependency so students can run every lab against a live authorized target immediately. Log-reading defender micro-exercises adapted to a throwaway local `python3 -m http.server` since Acunetix's logs aren't ours to read |
| 2026-08-27 | Consolidated the project into ONE folder: the git repo moved from `E:\Courses\ceh-diploma` to `E:\Work\ITgate\CEH_Course` (the Cowork-connected folder). Stale duplicates there (`sessions/`, old `design/`, `exercises/`, `knowledge_base/`, `PROJECT.md`, `DECISIONS.md`, empty `attacks/`/`testing/`/`scripts/`, stray `images.jpg`) deleted; the two folders that existed ONLY there were kept in place and are now inside the repo: `Resources/` (268 MB vendor PDFs) and `labs/kali-linux-2026.2-vmware-amd64*` (22 GB VM + 7z). Old `E:\Courses\ceh-diploma` deleted. `.gitignore` extended with `labs/kali-linux-*` | Two divergent copies of the same project (a stale working folder + the live repo) was a standing hazard - every session had to re-learn which one was real. Merging into the connected folder means Cowork opens the live repo by default. The VM/PDF folders were merged in place rather than copied because moving 22 GB across the bridge is not viable; the new ignore rule stops a future `git add -A` from trying to commit them |
| 2026-08-28 | Built Session 3 (Scanning & Enumeration): 27-page `docs/session-03/index.html` + full 7-doc package (session_plan, instructor_guide, student_guide, guided_lab in docs/session-03/; student_activity, quiz, homework + target_profile_template in exercises/session-03/). Reused shared ceh.css/session.js unchanged — no per-session CSS. Instructor approved the page-by-page outline before build (same gate as S1/S2) with "i approve" → taken as DC-promotion yes + keep ~250 min with documented absorb options | Phase 4, Session 3; spine = "recon was invisible, scanning is the loudest/most-detected attacker stage," every scan carries its SOC flip |
| 2026-08-28 | Session 3 built ~50% hands-on: pre-flight + 8 labs ≈ 124 min of ~256 (plan is +16 over 240, documented absorb options: move Lab 6 service-sweep to homework, or trim Lab 2 by 4 — never cut Lab 7 or Lab 2 core). Every tool taught with the 7-part frame (what/why/method/syntax/how-to-read/what-it-feeds/SOC-flip) | Instructor's standing "focus on methods" priority; matches S2's lab-heavy shape (S2 was 47%) |
| 2026-08-28 | Signature exercise = students write ONE working port-scan detection rule from packet/log evidence they generated in Lab 2. Worked example in Sigma (horizontal SYN scan: count(DestPort) by SourceAddress > 100 in 30s, Windows Firewall EventID 5156, tagged T1046/T1595); SPL and KQL skeletons for them to complete (their choice which). Lab 7 is the protected block — attack → capture → detect on the same packets | The prompt's named signature exercise; nothing else in the diploma closes the offense→defense loop this cleanly, and it is exactly the tier-1 SOC job |
| 2026-08-28 | Lab targets: promoted `WINSRV19-TGT01` to a domain controller for `ceh.lab` + joined `WIN10-TGT01` (closes topic_map "no AD lab" open item, unblocks S4 NTLM/Kerberos/LLMNR + S6 Mimikatz); installed SNMP on WINSRV19 with a deliberately weak community string; left SMB policy at modern defaults and teach WHY enum4linux is empty (Microsoft-confirmed: SMBv1/null sessions off by default Win10 FCU/Server 2019+) rather than re-enabling SMBv1. NFS/FTP/SMTP/RPC anchored on Metasploitable2. All changes edited IN PLACE in `labs/lab_design.md` (§"Session 3 target preparation"); automation added as `scripts/lab_s3_dc_setup.ps1` + `scripts/lab_s3_snmp_setup.ps1` (placeholders only, every secret prompted at runtime, credential-scanned clean) | The prompt's lab-prep asks; a pre-broken SMBv1 target teaches less than the modern-vs-2012 contrast; DC recommendation approved by instructor |
| 2026-08-28 | CORRECTED the Session 1/2 assumption that "all Nmap rooms are Premium." Re-verified every candidate live (logged-out + the authoritative `/api/v2/rooms/details` freeToUse field): FREE = `furthernmap` (the flagship "Nmap" room — switches, all scan types, NSE, evasion), `nmap02`, `networkservices`, `networkservices2`, `rustscan`, `vulnversity`, `basicpentestingjt`, `blue`; PREMIUM = `nmap` (The Basics), `nmap03`, `nmap04`, Wireshark rooms; PRIVATE = `enumeration`, `rpnmap`; CONFLICTING = `nmap01` (API says free, logged-out page blocks — flagged verify-on-day); DEAD = `nmapthebasics`. Recorded under the Scanning & Enumeration (S3) section of `design/practice_platforms.md` with the better API-based verification method. So S3 has a fully-free practice path — which matters more this session since the good rooms were assumed paywalled | The prompt flagged the free path as critical for S3 and asked to correct S1's wrong tier assumptions; the room HTML page returns false "Unauthorized" for some free rooms, so the API freeToUse field is the reliable check |
| 2026-08-28 | Session 3 visuals: 10 hand-authored inline SVG figures (5 click-to-reveal incl. the scan-type response matrix built from REAL captured packets, 1 animated handshake→SYN-scan diverge, host-discovery ARP-vs-router, enumeration funnel, SNMP MIB tree, evasion-preview matrix, S2→S6 continuity) PLUS 2 real captured-data JPGs generated from live scans run in-container against a veth lab target (`syn-vs-connect-capture.jpg` = the 3-scan side-by-side from real tshark output; `packet-noise-comparison.jpg` = real packet counts 13/103/708 — the noise argument). ~96 KB total. Defender-side screenshots (Win Event Viewer 5156/5157, Zeek conn.log, Suricata) deferred as labelled placeholders pending capture on the instructor's monitored Windows lab — the SVG real-capture panels carry the teaching meanwhile | Wireshark is "the star" of S3; captured genuine packet data rather than illustrating from memory. Lighter total diagram count than S2 (~24) — flagged in build_log as an open item, not padded with weak diagrams |
| 2026-08-28 | Verified Session 3 with the design_system §8 Playwright recipe (`viewport:` not `viewportSize:`) across 1400/1100/900/700/480px: 0 document overflow at every width, 0 elements wider than viewport outside a scroll container, 0 SVG text outside its viewBox, 20/20 interactive `data-node`s open exactly one `data-detail` (0 orphans), 0 console errors, both real JPGs load, 27 pages, snum P02–P27 contiguous. Local http-server check: `/session-03/index.html` + all asset paths return 200. No instructor-only text in the student HTML. Dashboard S3 card flipped to Delivered | Pre-publish gate, same as S1/S2. NOT yet git-committed/pushed — awaiting instructor review, then `git push` via GitHub Desktop (Cowork has no push credentials) |
| 2026-08-28 | Session 3 expanded to a four-OS "target zoo" per instructor request: added **WIN7-TGT01** (unpatched Windows 7, SMBv1 on, MS17-010 vulnerable) as the legacy-Windows target alongside Metasploitable2, Win10 and the Server 2019 DC. New diagram-forward page P04 "One scan, every OS" (lab topology + interactive same-scan-four-OS matrix + SMBv1/null-session per-OS matrix). Win7 woven into pre-flight, Lab 4 (the box where classic enum4linux output STILL works) and the Blue practice note. Page now 28 pages, 24/24 interactive nodes. Lab docs edited in place: `labs/lab_design.md` gained the target-zoo table, Metasploitable2 acquisition/import steps (Rapid7/SourceForge, VMDK, host-only, msfadmin, "never expose to a hostile network"), and a recommended-additional-vulnerable-machines table (Metasploitable3, Kioptrix, Stapler, VulnOS/SickOs, THM Blue; GOAD noted but out-of-scope). `labs/setup_guide.md` gained a Win7 build section and the WINSRV19 note updated from "no AD" to the DC-promotion prep | Instructor: students must learn to scan across all OS types; Win7 is the missing middle that makes "why the textbook SMB output is empty on modern Windows" concrete (it's where it's NOT empty), and completes the EternalBlue arc into S5. Instructor also asked for more diagrams / less prose — this change is diagram-led. Verified: Playwright §8 clean at all 5 widths, 28 pages, 24/24 nodes, 0 errors |
| 2026-08-28 | Added a Session 3 **practice range** (instructor downloaded the machines): **Metasploitable 3** (Win Server 2008 R2 — web-service + SNMP + SMB "Star Wars" account enum), **Kioptrix 1** (mod_ssl 2.8.4/OpenFuck + Samba 2.2.1a/trans2open — version→exploit reading), **Stapler 1** (enum4linux user-list dump + a service hidden on 12380). New diagram-forward page P27 "The practice range" with real per-machine scan+enum output (interactive), a skill-summary diagram, and host-only safety. Wired into the practice-plan page + homework (new task 4). Lab docs edited in place: `lab_design.md` "Recommended additional machines" section rewritten as a "Session 3 practice range (downloaded, in use)" with real port signatures + import steps; Kioptrix 2–5/VulnOS/SickOs/GOAD kept as still-optional. `setup_guide.md` gained VulnHub-OVA + Metasploitable3 import steps. Page now 29 pages, 27/27 interactive nodes. Real output verified against public walkthroughs (Rapid7, Steflan, VulnHub) | Instructor asked to add all three to the materials, update the practice labs, and show each machine's output as diagrams. §8 Playwright clean at all 5 widths, 29 pages, 27/27 nodes, 0 errors |
| 2026-08-28 | Built Session 4 (System Hacking I — Vulnerability Analysis, Authentication & Password Attacks): 29-page `docs/session-04/index.html` + full 7-doc package (session_plan, instructor_guide, student_guide, guided_lab, build_log in docs/session-04/; student_activity, quiz, homework, access_plan_template in exercises/session-04/). Reused shared ceh.css/session.js unchanged — no per-session CSS. Instructor approved the outline + all four gate decisions (build · approve seeding · Nessus Essentials primary · S5 bridge concept-only) before build, same gate as S1–S3 | Phase 4, Session 4; spine = "attackers don't break in, they log in" — first session that attacks a real domain (`ceh.lab`), every credential attack carries its auth-log SOC flip |
| 2026-08-28 | Session 4 built ~45% hands-on (pre-flight + 8 labs ≈ 117 min of ~259; +19 over 240 with documented absorb options: Lab 2 demo → homework, shadow-half of Lab 3 → homework, trim mechanism pages; protected: Labs 4/5/6 evidence, Lab 7 rule, Lab 8 deliverable). Every tool taught with the 7-part frame; mechanism (hashing/NTLM/Kerberos/taxonomy) built as diagrams BEFORE the tools | Instructor's standing "focus on methods" priority; matches the S2/S3 lab-heavy shape |
| 2026-08-28 | Signature exercise (Lab 7) = students write one working credential-attack detection rule from the 4625/4769/Responder evidence they generate in Labs 4–6. Worked example in Sigma (password spray: count(TargetUserName) by IpAddress ≥10 in 5m, EventID 4625 status 0xC000006A, tagged T1110.003); SPL + KQL skeletons; Kerberoast (4769 enc-type 0x17) + LLMNR offered as alternatives. Deliverable = the access plan (per host: CVE+CVSS+public-exploit? · creds+how · chosen way in), literally Session 5's input | The prompt's named signature exercise + deliverable; closes the offense→defense loop on the authentication surface, the tier-1→tier-2 SOC job |
| 2026-08-28 | Session 4 lab prep: new `scripts/lab_s4_dc_setup.ps1` (sibling to the S3 scripts, NOT a v2) with stages Kerberoast (SPN + weak pw on svc_backup that cracks; svc_sql SPN + strong pw = the never-cracks case), SprayPolicy (weak pw on a 3-of-5 subset, m.said distinct+strong, lockout 5/15), LocalAccounts (weak local accounts on Win7/Win10 for the SAM lab), CheckLLMNR (read-only Win10 status). Placeholders only, secrets prompted at runtime, credential-scanned clean. `labs/lab_design.md` + `setup_guide.md` edited in place with a "Session 4 target preparation" section + a `baseline-s4` snapshot. Scanner = Nessus Essentials (guided demo) | The prompt's lab-prep asks; the S3 seed gave svc_backup no SPN and one shared password, so Kerberoast/realistic-spray needed seeding; approved by instructor |
| 2026-08-28 | Session 4 currency corrections (verified against primary sources, taught as the "check before you quote" habit): Nessus Essentials free tier is now **5 IPs, not 16** (Tenable product page); LLMNR/NBT-NS still on by default on Win10/11 in 2026 (Microsoft ramping to mDNS, not disabled — Responder poisons mDNS too); Impacket is Fortra's (`impacket-*`); CVSS v4.0 exists (Nov 2023) but NVD/CEH lead with v3.1 (v4 reworked the vector); hashcat modes 1000/5600/13100/18200; rockyou ships gzipped. THM rooms API-verified: FREE = vulnerabilities101, openvas, crackthehash, hydra, attacktivedirectory (the free AD gem), networkservices2; PREMIUM/PRIVATE = hashingcrypto101, johntheripperbasics (johntheripper0 dead), passwordattacks, attackingkerberos, nessus, responder, rpvulnerabilityscanning(dead), holo. Recorded in `design/practice_platforms.md` §S4 | The prompt's currency + THM asks; the 16→5 Nessus change and the LLMNR-still-alive status materially change what we teach |
| 2026-08-28 | Session 4 visuals: 16 hand-authored inline SVG (7 click-to-reveal, 2 animated with replay — NTLM challenge-response + Kerberos ticket flow) covering all 10 must-have diagrams, PLUS 1 real captured-data JPG (`hashcat-crack.jpg` — a genuine in-container `hashcat -m 1000` run cracking fake lab NT hashes 4/4). The three Windows/domain captures (4625 spray, Responder, GetUserSPNs) need the instructor's monitored domain (no Windows/AD in the container) and ship as labelled placeholders — same pattern as S3's deferred defender screenshots. Shared arrow markers moved to a hidden top-of-article `<defs>` SVG so removing any diagram can't break arrows. Verified: design_system §8 Playwright clean at 1400/1100/900/700/480px (0 overflow, 0 SVG-escape, 28/28 interactive nodes, 0 real console errors), 29 pages, no instructor-only text | Instructor's standing "diagrams over words, beat S3 density" preference; captured what the container genuinely can and flagged the rest honestly rather than faking domain output |
